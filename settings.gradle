pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'staffingservice'

tasks.bootJar {
    archiveFileName.set("staffingservice.jar")
}
